# Uniqueness Validator
br.com.alexandreaquiles.casadocodigo.infra.errors.validation.Unique.message=o {field} de {entity} deve ser único

# Autor
autor.email.unico = email do autor deve ser único: ${validatedValue}

# Categoria
categoria.nome.unico = nome da categoria deve ser único: ${validatedValue}

#Livro
livro.titulo.unico = título do livro deve ser único: ${validatedValue}
livro.isbn.unico = ISBN do livro deve ser único: ${validatedValue}
